# build output
dist/
# generated types
.astro/
# wrangler / cloudflare pages local dev cache
.wrangler/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# local-only design study tools (kept out of source on purpose: this one loads faces from the
# Google Fonts CDN, and source carries no Google Fonts origin. See DECISIONS 2026-07-25)
typography-playground.html

# local-only design study, same reasoning: the HackTheBox difficulty palette study lives under public/
# so the dev server serves it with the self-hosted fonts (2026-09-07). Never a source file.
public/htb-difficulty-palette-study*.html
